# 匹配服务依赖 (与现有 Flask 服务独立)

# ── Web框架 ──
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# ── HTTP客户端 ──
httpx>=0.25.0

# ── PDF解析 ──
PyPDF2>=3.0.0

# ── 数据验证 ──
pydantic>=2.0.0

# ── 文件上传 ──
python-multipart>=0.0.6

# ── 认证与安全 ──
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
