# 就业服务平台 · 校园版 —— 辅导员手册

> 本手册面向**没有任何编程基础**的老师。按步骤操作即可，遇到问题直接查「常见问题」。

---

## 一、这套系统是什么

- 一个部署在学院服务器上的**就业信息网站**，全院学生用浏览器就能访问
- 自动从各大招聘平台采集最新岗位（社招 + 公务员/事业编）
- 学生可以浏览岗位、上传简历做 **AI 智能匹配**、订阅新岗位邮件通知

**费用说明：浏览岗位、订阅推送完全免费。** AI 匹配功能（简历分析、智能推荐）使用学生**自己的** DeepSeek API Key，费用由学生自己承担（每次约几分钱），学院零成本。

---

## 二、第一次部署（只做一次，约 10 分钟）

### 第 1 步：解压

把 `就业服务平台-校园版.zip` 复制到服务器上，**右键 → 全部解压**，建议解压到 `D:\就业服务平台`（路径不要有中文括号）。

> ⚠️ 必须完整解压整个文件夹，**不要只把启动文件拖出来**。

### 第 2 步：环境预检（可选但推荐）

双击 **`环境预检.bat`**，等它跑完。如果全部显示 ✅，直接下一步；有 ❌ 就截图发给技术支持。

### 第 3 步：首次启动

双击 **`启动系统.bat`**：

1. 第一次启动会要求**设置管理员密码**（输入时屏幕不显示，这是正常的）。请设置一个你能记住的密码，记在本子上。
2. 看到窗口里出现访问地址，说明启动成功：

```
  学生入口：http://192.168.x.x:8000/
  管理后台：http://192.168.x.x:8000/admin.html
```

3. 把**学生入口地址**发到学生群即可。

> ⚠️ 这个黑色窗口就是服务本体，**关窗 = 停服务**。想长期运行请看第 4 步。

### 第 4 步：开机自启（强烈建议）

**右键** `安装开机自启.bat` → **以管理员身份运行**。完成后，服务器重启服务也会自动起来，平时不用再管。

### 第 5 步：配置采集关键词

1. 浏览器打开 **管理后台** 地址，用 `admin` + 你设置的密码登录
2. 左侧「爬虫系统 → 社招爬虫」：填写搜索关键词（如 `软件开发, 会计, 市场营销`），开启定时采集，点「保存配置」
3. 点「立即采集一次」验证能采到数据
4. 「公考爬虫」同理（默认采集全国公务员职位，一般不用改）

### 第 6 步：配置邮件服务（用于订阅推送）

学生订阅新岗位通知需要学院提供一个发件邮箱（推荐用 QQ 邮箱或 163 邮箱）：

1. 登录该邮箱网页版，开启 **SMTP 服务**并获取**授权码**（注意：不是登录密码）
   - QQ 邮箱：设置 → 账户 → POP3/SMTP 服务 → 生成授权码
   - 163 邮箱：设置 → POP3/SMTP → 开启并获取授权码
2. 管理后台 →「系统设置」→ 填入 SMTP 信息（主机如 `smtp.qq.com`，端口 `465`）

---

## 三、日常使用

| 你要做什么 | 怎么做 |
|---|---|
| 看看系统是否正常 | 管理后台 →「数据源健康」，一页看全 |
| 手动补采一次岗位 | 「爬虫系统 → 社招爬虫」→ 立即采集一次 |
| 查看有多少学生注册 | 「用户管理」 |
| 改管理员密码 | 「系统设置 → 修改管理员密码」 |
| 服务器关机/重启后 | 装了开机自启就不用管；没装就双击「启动系统.bat」 |

**学生侧操作**（可转发给学生）：

- 浏览岗位：打开网址即可，不用注册
- AI 简历匹配：注册登录 →「API Key」页填入自己的 DeepSeek Key（页面里有图文教程）→「简历匹配」页上传简历
- 订阅新岗位邮件：「订阅推送」页上传简历 + 绑定邮箱

---

## 四、Cookie 维护（智联/猎聘需要，约 1-2 周一次）

前程无忧、实习僧、公考职位**不需要** Cookie。智联招聘、猎聘网需要定期更新 Cookie：

1. 在**你自己的办公电脑**上（不用碰服务器），打开系统文件夹里的 `Cookie采集工具.bat`
2. 浏览器会自动打开招聘网站，**手动完成登录**后回到窗口按回车
3. 工具会在 `cookies` 文件夹里生成对应文件
4. 打开管理后台 →「爬虫系统 → Cookie 管理」→ 把文件上传到对应平台

> 「数据源健康」页面会用红黄绿标出 Cookie 状态，红色就是该更新了。

---

## 五、常见问题

**Q：学生打不开网址？**
1. 看服务器上的黑色窗口还在不在（不在就双击「启动系统.bat」）
2. 学生的电脑必须连着学院内网
3. 右键「启动系统.bat」→ 以管理员身份运行一次（让防火墙放行）

**Q：打开网站没有岗位数据？**
管理后台 → 社招爬虫 → 立即采集一次。首次部署后需要手动采一次。

**Q：学生问 AI 匹配报错"未提供 API Key"？**
让学生去「API Key」页，按页面里的图文教程申请并填入自己的 DeepSeek Key。

**Q：黑色窗口里滚动的红字是什么？**
大多是某个招聘网站临时的反爬拦截，系统会自动跳过，不影响其他平台。只有「数据源健康」里对应任务标红才需要处理。

**Q：想把系统交给别的老师/换一台服务器？**
把整个文件夹原样拷贝过去，双击「启动系统.bat」即可。数据都在 `data` 文件夹里，跟着走。

**Q：数据会丢吗？**
系统每天凌晨自动备份 `data` 目录到 `data_backups`，保留最近 7 份。

---

## 六、技术支持

遇到本手册解决不了的问题：
1. 双击 `环境预检.bat`，把生成的 `预检报告.txt` 发给技术支持
2. 把黑色窗口里的报错截图一并附上
